name: 🤖 Generation Individual iFlow Documentation

on:
  workflow_dispatch:
    inputs:
      package_id:
        description: "The ID of the CPI Integration Package (e.g., 'PDFcoveter')."
        required: true
        type: string

jobs:
  document:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Dependencies (jq only)
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Generate Individual iFlow Documentation via OpenAI API
        id: documentation_step
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          PKG_ID: ${{ github.event.inputs.package_id }}
          HEADING_COLOR: "#1f4e79"
        run: |
          # --- Configuration ---
          API_URL="https://api.openai.com/v1/chat/completions"
          MODEL_NAME="gpt-4o-mini"
          BASE_PKG_DIR="cpi-artifacts/$PKG_ID"

          if [ ! -d "$BASE_PKG_DIR" ]; then
            echo "::error::Package directory not found at: $BASE_PKG_DIR"
            exit 1
          fi

          # --- INTEGRATED TECHNICAL PROMPT ---
          # Derived from project template requirements [cite: 4, 26, 32, 38, 50, 64, 69]
          SYSTEM_PROMPT="You are a senior SAP CPI Technical Architect. Your task is to analyze ALL provided code and configuration files from the SINGLE iFlow provided and synthesize them into ONE consolidated technical report. 
          
          CRITICALLY: 
          1. Use ONLY inline HTML tags for all headings (H1 and H2) to ensure the dark blue color ('${{ env.HEADING_COLOR }}') is applied.
          2. For the 'Integration Architecture' (Section 2.1), you MUST use a Mermaid 'graph TD' block. Ensure the Mermaid code uses REAL newlines so it renders correctly.
          3. Start with a Table of Contents (TOC). List items with <br> for spacing.
          4. End the TOC section with the exact marker '---TOC-END-PAGE-BREAK---'.

          Structure:
          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">1. Introduction</h1>
          [cite_start]1.1 Purpose: Explain the solution conceptualized[cite: 28]. 
          [cite_start]1.2 Scope: Outline data flows and integration touchpoints[cite: 30].
          
          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">2. Integration Overview</h1>
          [cite_start]2.1 Integration Architecture: Provide a Mermaid graph[cite: 35].
          [cite_start]2.2 Integration Components: List Sender/Receiver and Adapters[cite: 37].
          
          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">3. Integration Scenarios</h1>
          [cite_start]3.1 Scenario Description: Step-by-step logic[cite: 39].
          [cite_start]3.2 Data Flows: Explain Mapping (Field to Field) and Groovy/XSLT logic[cite: 41, 45].
          [cite_start]3.3 Security Requirements: Credentials and Auth mechanisms[cite: 48].
          
          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">4. Error Handling and Logging</h1>
          [cite_start]Describe exception subprocesses and logging scripts[cite: 51, 53].
          
          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">5. Testing Validation</h1>
          [cite_start]Provide Test Case IDs and Scenarios[cite: 67].
          
          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">6. Reference Documents</h1>
          [cite_start]List analyzed files (iFlowContent.xml, scripts, etc.)[cite: 21, 23]."

          ALL_DOCS_GENERATED=false

          # 1. Process iFlows (Fix: Process Substitution ensures variable persistence)
          while IFS= read -r IFLOW_DIR; do
            
            IFLOW_ROOT_NAME=$(echo "$IFLOW_DIR" | sed "s|^$BASE_PKG_DIR/||" | cut -d/ -f1)
            FINAL_IFLOW_DIR="$BASE_PKG_DIR/$IFLOW_ROOT_NAME"
            OUTPUT_FILE="$FINAL_IFLOW_DIR/${IFLOW_ROOT_NAME}_Summary.md"
            
            echo "🚀 Processing iFlow: $IFLOW_ROOT_NAME"
            
            QUERY_FILE="user_query.tmp"
            echo -e "Synthesize a technical report for iFlow '$IFLOW_ROOT_NAME'. Source artifacts below:\n" > "$QUERY_FILE"

            while IFS= read -r -d '' INPUT_FILE; do
              echo -e "\n\n--- START ARTIFACT: $INPUT_FILE ---\n" >> "$QUERY_FILE"
              cat "$INPUT_FILE" >> "$QUERY_FILE"
              echo -e "\n--- END ARTIFACT: $INPUT_FILE ---\n" >> "$QUERY_FILE"
            done < <(find "$IFLOW_DIR" -type f \( -name 'iFlowContent.xml' -o -name '*.groovy' -o -name '*.xslt' -o -name '*.iflw' \) -print0)

            # Fix: Create JSON payload using --rawfile to handle massive text safely
            PAYLOAD_FILE="payload.json"
            jq -n --arg system "$SYSTEM_PROMPT" --rawfile query "$QUERY_FILE" --arg model "$MODEL_NAME" \
              '{model: $model, messages: [{role: "system", content: $system}, {role: "user", content: $query}], temperature: 0.1}' > "$PAYLOAD_FILE"

            RETRY_COUNT=0
            while [ "$RETRY_COUNT" -lt 3 ]; do
              API_RESPONSE=$(curl -s -X POST "$API_URL" -H "Content-Type: application/json" -H "Authorization: Bearer $OPENAI_API_KEY" -d @"$PAYLOAD_FILE")
              GENERATED_TEXT=$(echo "$API_RESPONSE" | jq -r ".choices[0].message.content // empty")

              if [ -n "$GENERATED_TEXT" ] && [ "$GENERATED_TEXT" != "null" ]; then
                # Fix: Preserve internal newlines for Mermaid diagrams
                GENERATED_TEXT_CLEANED=$(echo "$GENERATED_TEXT" | sed '1{/^```/d}; ${/^```/d}')
                
                AUTHOR=$(git log -1 --pretty=format:'%an')
                DATE=$(date +'%Y-%m-%d')
                CLEAN_NAME=$(echo "$IFLOW_ROOT_NAME" | sed 's/_/ /g')

                SAP_LOGO="https://raw.githubusercontent.com/Rohancherian783/trail1/main/assets/sap_logo.png"
                MM_LOGO="https://raw.githubusercontent.com/Rohancherian783/trail1/main/assets/motiveminds_logo.png"
                LOGO_HEADER="<div style=\"float: left;\"><img src=\"$SAP_LOGO\" width=\"150\" height=\"60\"/></div><div style=\"float: right;\"><img src=\"$MM_LOGO\" width=\"150\" height=\"55\"/></div><div style=\"clear: both;\"></div>"
                
                COVER_PAGE="${LOGO_HEADER}<div style=\"height: 80px;\"></div><h1 style=\"color: ${{ env.HEADING_COLOR }}; text-align: center; font-size: 3em;\">$CLEAN_NAME</h1>"
                COVER_PAGE+="<h2 style=\"text-align: center;\">Technical Specification</h2>"
                COVER_PAGE+="<div style=\"height: 100px;\"></div><div style=\"text-align: center;\"><table border=\"1\" style=\"margin: 0 auto; border-collapse: collapse;\">"
                COVER_PAGE+="<tr><td style=\"padding: 10px;\"><b>Author</b></td><td style=\"padding: 10px;\">$AUTHOR</td></tr>"
                COVER_PAGE+="<tr><td style=\"padding: 10px;\"><b>Date</b></td><td style=\"padding: 10px;\">$DATE</td></tr>"
                COVER_PAGE+="</table></div>"
                
                FINAL_DOC="${COVER_PAGE}<div style=\"page-break-after: always;\"></div>\n\n${LOGO_HEADER}\n${GENERATED_TEXT_CLEANED}"
                
                # Replace TOC marker with Logo + Page Break
                SECTION_BREAK="\n<div style=\"page-break-before: always;\"></div>\n${LOGO_HEADER}\n"
                FINAL_DOC=$(echo -e "$FINAL_DOC" | sed "s|---TOC-END-PAGE-BREAK---|$(echo "$SECTION_BREAK" | sed 's/[&/]/\\&/g')|g")

                echo -e "$FINAL_DOC" > "$OUTPUT_FILE"
                ALL_DOCS_GENERATED=true
                echo "✅ Saved: $OUTPUT_FILE"
                break
              else
                RETRY_COUNT=$((RETRY_COUNT + 1))
                sleep 5
              fi
            done
            rm -f "$QUERY_FILE" "$PAYLOAD_FILE"
          done < <(find "$BASE_PKG_DIR" -type f \( -name 'iFlowContent.xml' -o -name '*.iflw' \) -printf '%h\n' | sort -u)

          echo "docs_generated=$ALL_DOCS_GENERATED" >> $GITHUB_OUTPUT

      - name: Commit and Push
        if: steps.documentation_step.outputs.docs_generated == 'true'
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          PKG_ID="${{ github.event.inputs.package_id }}"
          git add "cpi-artifacts/$PKG_ID/**/*_Summary.md"
          if ! git diff --cached --quiet; then
              git commit -m "🤖 DOCS: Generated formatted technical specs for $PKG_ID"
              git push
          fi
