name: 🤖 Generation Individual iFlow Documentation
on:
  workflow_dispatch:
    inputs:
      package_id:
        description: "The ID of the CPI Integration Package (e.g., 'PDFcoveter')."
        required: true
        type: string

jobs:
  document:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Dependencies (jq only)
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Generate Individual iFlow Documentation via OpenAI API
        id: documentation_step
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          PKG_ID: ${{ github.event.inputs.package_id }}
          HEADING_COLOR: "#1f4e79"
        run: |
          # --- Configuration ---
          API_URL="https://api.openai.com/v1/chat/completions"
          MODEL_NAME="gpt-4o-mini"
          BASE_PKG_DIR="cpi-artifacts/$PKG_ID"

          if [ ! -d "$BASE_PKG_DIR" ]; then
            echo "::error::Package directory not found at: $BASE_PKG_DIR"
            exit 1
          fi

          # --- REFINED TECHNICAL PROMPT ---
          # We use explicit instructions for "Newlines" and "Vertical alignment"
          SYSTEM_PROMPT="You are a senior SAP CPI Technical Architect. Your task is to analyze the provided artifacts and synthesize a consolidated technical report. 
          
          CRITICAL FORMATTING RULES:
          1. Use ONLY inline HTML for H1/H2 headings (Color: ${{ env.HEADING_COLOR }}).
          2. NEVER put multiple bullet points or table rows on the same line. 
          3. For all tables, ensure a proper Markdown header, a separator line (---), and one row per line.

          **MANDATORY FIRST SECTION: TABLE OF CONTENTS (TOC)**
          <h1 style=\"color: ${{ env.HEADING_COLOR }}; font-size: 2.5em;\">Table of Contents</h1>
          1. Introduction <br>
          2. Integration Overview <br>
          3. Integration Scenarios <br>
          4. Error Handling and Logging <br>
          5. Testing Validation <br>
          6. Reference Documents <br>
          ---TOC-END-PAGE-BREAK---

          **REQUIRED SECTIONS:**
          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">1. Introduction</h1>
          1.1 Purpose: Briefly state the purpose.
          1.2 Scope: Define the integration scope.

          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">2. Integration Overview</h1>
          2.1 Integration Architecture: Use a Mermaid 'graph TD' block.
          2.2 Integration Components:
          You MUST use this EXACT table format:
          | Component Type | Name/Details | Description |
          | :--- | :--- | :--- |
          | Sender System | [Name] | [Description] |
          | Receiver System | [Name] | [Description] |
          | Adapter | [Type] | [Usage Details] |

          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">3. Integration Scenarios</h1>
          3.1 Scenario Description: Use a vertical numbered list (1, 2, 3) for the logic flow.
          3.2 Data Flows: 
          - **Mapping Logic**: Describe the mapping artifacts.
          - **XSLT**: List XSLT transformations.
          - **Groovy Scripts**: Explain the specific role of each script.
          (Each item MUST be on its own new line with a bullet point).

          3.3 Security Requirements:
          Provide a Markdown table with columns: | Security Aspect | Details |. 

          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">4. Error Handling and Logging</h1>
          Describe exception sub-processes or logging scripts used.

          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">5. Testing Validation</h1>

          <h1 style=\"color: ${{ env.HEADING_COLOR }};\">6. Reference Documents</h1>
          - List all analyzed files (e.g., iFlowContent.xml, .groovy files) as a vertical bulleted list."

          ALL_DOCS_GENERATED=false
          while IFS= read -r IFLOW_DIR; do
            IFLOW_ROOT_NAME=$(echo "$IFLOW_DIR" | sed "s|^$BASE_PKG_DIR/||" | cut -d/ -f1)
            FINAL_IFLOW_DIR="$BASE_PKG_DIR/$IFLOW_ROOT_NAME"
            OUTPUT_FILE="$FINAL_IFLOW_DIR/${IFLOW_ROOT_NAME}_Summary.md"

            echo "🚀 Processing iFlow: $IFLOW_ROOT_NAME"

            QUERY_FILE="user_query.tmp"
            echo -e "Analyze the following artifacts for iFlow: $IFLOW_ROOT_NAME\n" > "$QUERY_FILE"
            
            while IFS= read -r -d '' INPUT_FILE; do
              echo -e "\n--- ARTIFACT: $(basename $INPUT_FILE) ---\n" >> "$QUERY_FILE"
              cat "$INPUT_FILE" >> "$QUERY_FILE"
            done < <(find "$IFLOW_DIR" -type f \( -name 'iFlowContent.xml' -o -name '*.groovy' -o -name '*.xslt' -o -name '*.iflw' \) -print0)

            PAYLOAD_FILE="payload.json"
            jq -n --arg system "$SYSTEM_PROMPT" --rawfile query "$QUERY_FILE" --arg model "$MODEL_NAME" \
              '{model: $model, messages: [{role: "system", content: $system}, {role: "user", content: $query}], temperature: 0.1}' > "$PAYLOAD_FILE"

            RETRY_COUNT=0
            while [ "$RETRY_COUNT" -lt 3 ]; do
              API_RESPONSE=$(curl -s -X POST "$API_URL" -H "Content-Type: application/json" -H "Authorization: Bearer $OPENAI_API_KEY" -d @"$PAYLOAD_FILE")
              GENERATED_TEXT=$(echo "$API_RESPONSE" | jq -r ".choices[0].message.content // empty")
              
              if [ -n "$GENERATED_TEXT" ] && [ "$GENERATED_TEXT" != "null" ]; then
                GENERATED_TEXT_CLEANED=$(echo -e "$GENERATED_TEXT" | sed -E '1s/^\s*```(markdown|text)?//' | sed -E '$s/\s*```\s*$//')

                AUTHOR=$(git log -1 --pretty=format:'%an')
                DATE=$(date +'%Y-%m-%d')
                CLEAN_NAME=$(echo "$IFLOW_ROOT_NAME" | sed 's/_/ /g')

                SAP_LOGO="https://raw.githubusercontent.com/Rohancherian783/trail1/main/assets/sap_logo.png"
                MM_LOGO="https://raw.githubusercontent.com/Rohancherian783/trail1/main/assets/motiveminds_logo.png"
                LOGO_HEADER="<div style=\"width: 100%;\"><div style=\"float: left;\"><img src=\"$SAP_LOGO\" width=\"150\" height=\"60\"/></div><div style=\"float: right;\"><img src=\"$MM_LOGO\" width=\"150\" height=\"55\"/></div><div style=\"clear: both;\"></div></div>"

                COVER_PAGE="${LOGO_HEADER}<div style=\"height: 100px;\"></div><h1 style=\"color: ${{ env.HEADING_COLOR }}; text-align: center; font-size: 3.5em;\">$CLEAN_NAME</h1>"
                COVER_PAGE+="<h2 style=\"text-align: center; color: #666;\">Technical Specification Document</h2>"
                COVER_PAGE+="<div style=\"height: 150px;\"></div><div style=\"text-align: center;\"><table style=\"margin: 0 auto; border: 1px solid #ccc; border-collapse: collapse;\">"
                COVER_PAGE+="<tr><th style=\"padding: 10px; border: 1px solid #ccc; background: #f9f9f9;\">Author</th><td style=\"padding: 10px; border: 1px solid #ccc;\">$AUTHOR</td></tr>"
                COVER_PAGE+="<tr><th style=\"padding: 10px; border: 1px solid #ccc; background: #f9f9f9;\">Date</th><td style=\"padding: 10px; border: 1px solid #ccc;\">$DATE</td></tr>"
                COVER_PAGE+="</table></div>"

                FINAL_DOC="${COVER_PAGE}<div style=\"page-break-after: always;\"></div>\n\n${LOGO_HEADER}\n${GENERATED_TEXT_CLEANED}"
                
                SECTION_BREAK="\n<div style=\"page-break-before: always;\"></div>\n${LOGO_HEADER}\n"
                FINAL_DOC=$(echo -e "$FINAL_DOC" | sed "s|---TOC-END-PAGE-BREAK---|$(echo "$SECTION_BREAK" | sed 's/[&/]/\\&/g')|g")
                
                echo -e "$FINAL_DOC" > "$OUTPUT_FILE"
                ALL_DOCS_GENERATED=true
                echo "✅ Documentation generated for $IFLOW_ROOT_NAME"
                break
              else
                RETRY_COUNT=$((RETRY_COUNT + 1))
                sleep 5
              fi
            done
            rm -f "$QUERY_FILE" "$PAYLOAD_FILE"
          done < <(find "$BASE_PKG_DIR" -type f \( -name 'iFlowContent.xml' -o -name '*.iflw' \) -printf '%h\n' | sort -u)
          
          echo "docs_generated=$ALL_DOCS_GENERATED" >> $GITHUB_OUTPUT

      - name: Commit and Push Documentation
        if: steps.documentation_step.outputs.docs_generated == 'true'
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          PKG_ID="${{ github.event.inputs.package_id }}"
          git add "cpi-artifacts/$PKG_ID/**/*_Summary.md"
          if ! git diff --cached --quiet; then
              git commit -m "🤖 DOCS: Professional Technical Specs for $PKG_ID"
              git push
          else
              echo "No changes to commit."
          fi
