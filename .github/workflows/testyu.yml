name: Test Minimal Heredoc

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:

      - name: Create python file safely
        run: |
          mkdir -p .github/scripts

          cat << 'EOF' > .github/scripts/test.py
#!/usr/bin/env python3
import sys
import re
print("Heredoc works!")
EOF

      - name: Show content
        run: cat .github/scripts/test.py




